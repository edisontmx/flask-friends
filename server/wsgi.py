from app import app

if __name__ == "__main__":
    app.run()

#Gunicorn and WSGI (web service gateway interface) are both components  used  in deploying and serving python web aplications, particulary those built with web frameworks like flask and django